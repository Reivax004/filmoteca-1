<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails du film</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container my-5">
    <h1 class="text-center text-primary">Détails du film</h1>
    <div class="card shadow-sm my-4">
        <div class="card-body">
            <h2 class="card-title text-success">{{ film.title|escape }}</h2>
            <p><strong>Année :</strong> {{ film.year|escape }}</p>
            <p><strong>Type :</strong> {{ film.type|escape }}</p>
            <p><strong>Réalisateur :</strong> {{ film.director|escape }}</p>
            <p><strong>Synopsis :</strong> {{ film.synopsis|nl2br|escape }}</p>
        </div>
    </div>
    <div class="text-center">
        <a href="/film/list" class="btn btn-primary">Retour à la liste</a>
    </div>
</div>
</body>
</html>
